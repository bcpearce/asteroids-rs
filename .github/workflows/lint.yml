name: prek
on:
    push:
        branches: [main]
    pull_request:

permissions:
    contents: read

jobs:
    prek:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - uses: j178/prek-action@v1
